// import { createSelector } from "@reduxjs/toolkit";
// import { rentAdapter } from './slice'
// const { selectAll, selectEntities } = rentAdapter.getSelectors();
// const selectRentState = (state) => state.rental;
// export const selectRentList = createSelector(selectRentState, selectAll);
// export const selectRentEntities= createSelector(selectRentState, selectEntities);
